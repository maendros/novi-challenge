import { AlphanumericMinThreeCharactersDirective } from './alphanumeric-min-three-characters.directive';

describe('AlphanumericMinThreeCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumericMinThreeCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
